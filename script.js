function startGame() {
  const playerOne = 0;
  const playertwo = 1;

  function createBoard() {}
  const arra1 = [0, 0, 0];
  const arra2 = [0, 0, 0];
  const arra3 = [0, 0, 0];

  function getUserInput() {}
}
// player selects the area

// player1 selects the arra1[1]
function playerSelects() {}
//player2 selecter arr2[1]

buttonstart.startGame();
buttonrestart.restartGame();

//player2 selector

function resetGame() {}

function switchPlayer(playerOne) {
  playerOne = 1;
}

//This is the main game function
